## Usage


```
git clone https://github.com/android-build-system/update_verifier verifier

cd verifier

pip3 install -r requirements.txt

python3 update_verifier.py lineageos_pubkey /path/to/zip
```
